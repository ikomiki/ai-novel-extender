import App from "./App.svelte";

const app = new App({
  // target: document.body,
  // target: document.getElementById("retryoptions"),
  // target: document.getElementById("modelinfo"),
  target: document.getElementById("options_usermod_history"),
  props: {},
});

export default app;
